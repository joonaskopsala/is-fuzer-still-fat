'use client'

import { Typography } from '@mui/material'

const Title = () => {
  return (
    <Typography variant="h1" textAlign="center" padding={2}>
      {'Is Fuzer still fat?'}
    </Typography>
  )
}

export { Title }
